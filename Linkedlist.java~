class Node
	int data;
	node next;
}

class Linkedlist
{
	Node head;
	
	void getNewNode(int data)
	{
		Node newNode=new Node();
		newNode.data=data;
		return newNode;
	}
	void insertAtHead(int data)
	{
		node newNode=getNewNode(data);
		newNode.next=head;
		head=newNode();
	}

	void insertAtN(int data)
	{
		
	}

}
